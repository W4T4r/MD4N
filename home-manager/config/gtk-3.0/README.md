# GTK 3

GTK 3 settings shared across the desktop configuration.

Keep `bookmarks` local-only. The directory is linked into place, so a machine-local
`home-manager/config/gtk-3.0/bookmarks` file can be used without tracking it in Git.
