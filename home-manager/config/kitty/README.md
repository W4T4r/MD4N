# Kitty

Shared Kitty terminal configuration managed through the repository config tree.

This directory is symlinked into `~/.config/kitty` by Home Manager.
Keep the shared terminal behavior here and avoid hard-coding personal or proprietary font choices in the distributed config.
