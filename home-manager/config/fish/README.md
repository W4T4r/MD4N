# Fish

Shared Fish shell configuration shipped by the repository.
`config.fish` itself is generated by Home Manager when `programs.fish.enable = true`.
Repository-owned startup behavior lives under [`conf.d/`](conf.d/README.md).
Put machine-local extras under `~/.config/fish/conf.d/local/` instead of this directory.
