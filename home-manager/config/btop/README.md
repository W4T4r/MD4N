# btop

Shared `btop` configuration.
The directory is linked out-of-store into `~/.config/btop`, so changes made inside `btop` persist directly back into this repository checkout.
Theme files are stored in the `themes/` subdirectory.
