# Niri

Shared Niri configuration tracked in Git.
The whole directory is linked directly from the repository into `~/.config/niri`.
`outputs.kdl` is generated locally into `~/.config/niri/`.
`browser.sh` is generated locally into `~/.config/md4n/niri/` and is called with an absolute path.
