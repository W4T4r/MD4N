# Screenshots

This directory contains selected screenshots used by the project documentation.

Only keep images here that are intentionally published as part of the repository README or related docs.
Prefer a small, curated set over dumping every local capture into the repository.
